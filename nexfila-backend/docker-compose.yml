services:

  backend:
    build: .
    ports:
      - "8080:8080"
    networks:
      - nexfila-network

  frontend:
    build: ../nexfila-frontend
    ports:
      - "3000:80"
    networks:
      - nexfila-network

networks:
  nexfila-network:
    driver: bridge